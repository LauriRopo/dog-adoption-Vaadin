package com.example.application.entity;

public enum DogStatus {
    AVAILABLE,   // Dog is available for adoption
    ADOPTED,     // Dog has been adopted
    PENDING      // Dog is under consideration for adoption
}
